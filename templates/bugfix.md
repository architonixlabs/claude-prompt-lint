# Bugfix prompt template

**What's broken:** <one-line symptom — what you observe>

**Where:** <file path : function / line, or the component>

**Repro / trigger:** <the exact input, request, or steps that cause it>

**Error / wrong output:**
```
<paste the error, stack trace, or the incorrect result>
```

**Expected behavior:** <what should happen instead>

**Constraints:** <don't change the public API / keep it backward-compatible / etc.>

**Done when:** <a test that passes, or the specific output you expect>
